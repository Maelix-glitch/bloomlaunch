{
  "name": "bloomlaunch",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Bloom — a cinematic launch experience. One authored world from first dark frame to final signature.",
  "scripts": {
    "dev": "vite",
    "build": "npm run typecheck && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "smoke": "node scripts/smoke.mjs"
  },
  "devDependencies": {
    "jsdom": "^30.1.0",
    "typescript": "^5.6.3",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "@fontsource-variable/fraunces": "^5.2.5",
    "@fontsource-variable/inter": "^5.2.5"
  }
}
